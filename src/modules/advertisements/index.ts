/**
 * Advertisements — creative assets an advertiser registers on the platform.
 *
 * Distinct from `orders`, which books a creative onto a listing. The router is
 * the whole public surface today.
 */
export { advertisementRouter } from './advertisements.routes';
