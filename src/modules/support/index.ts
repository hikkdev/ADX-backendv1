/**
 * Support — user-raised tickets and their message threads.
 *
 * Nothing here is called by another module today, so the router is the whole
 * public surface.
 */
export { supportRouter } from './support.routes';
